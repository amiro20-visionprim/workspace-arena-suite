# Vision Prime SUITE — Decision Log

این فایل برای جلوگیری از تصمیم‌های حدسی و گم‌شدن مسیر است. هیچ تصمیم معماری/UX حساس بدون ثبت اینجا نهایی تلقی نمی‌شود.

| ID | تاریخ | تصمیم | وضعیت | دلیل | اثر/وابستگی | مالک |
|---|---|---|---|---|---|---|
| D-001 | 2026-07-25 | Modular Monolith با Laravel 12 | قطعی | پیچیدگی عملیاتی کمتر و مرزبندی کافی | کل backend | Product/Tech |
| D-002 | 2026-07-25 | فارسی و RTL-first؛ انگلیسی secondary | قطعی | بازار اولیه ایران | همه UIها | Product/Design |
| D-003 | 2026-07-25 | نمایش جلالی؛ ذخیره UTC/Gregorian | قطعی | تجربه محلی بدون آسیب به داده | UI/date utilities | Product/Tech |
| D-004 | 2026-07-25 | Automation در سطح Site و Policy-driven | قطعی | کنترل ریسک و نیاز مشتریان متفاوت | commands/review/plugin | Product/Security |
| D-005 | 2026-07-25 | L0 تا L4؛ Autopilot نامحدود ممنوع | قطعی | جلوگیری از تغییر پرریسک و حفظ اعتماد | automation UX/API | Product/Security |
| D-006 | 2026-07-25 | AI هیچ‌وقت مستقیم publish نمی‌کند | قطعی | ریسک برند، کیفیت و امنیت | AI Gateway | Product/Security |
| D-007 | 2026-07-25 | مدل استقرار Hybrid است: SaaS پیش‌فرض و Private deployment برای Enterprise | قطعی | تعادل سرعت تجاری‌سازی با نیاز مشتریان بزرگ به کنترل و ایزولیشن | tenancy, deployment, operations | Product/Tech |
| D-008 | 2026-07-25 | Commandهای R3 فقط برای Enterprise و با guardrailهای سخت‌گیرانه می‌توانند خودکار شوند | قطعی | پاسخ به نیاز اتوماسیون بالا بدون ایجاد ریسک عمومی | automation, command policy | Product/Security |
| D-009 | 2026-07-25 | Billing آنلاین در نسخه نهایی اولیه داخل Scope اجرایی نیست؛ entitlement و آماده‌سازی معماری لحاظ می‌شود | قطعی | تمرکز لانچ بر ارزش عملیاتی و فروش سازمانی/قراردادی | settings, plans, commercial ops | Product/Commercial |
| D-010 | 2026-08-13 | لندینگ‌های اختصاصی فقط برای Tier 1 (آژانس، فروشگاه) و Tier 2 (کلینیک، آموزش، سفر/هتلداری، املاک، خدمات حرفه‌ای) ساخته می‌شوند | قطعی | Tier 3 (اینستاگرامی، کسب‌وکار خرد) فاقد پیش‌نیازهای سخت (وردپرس+GSC) و قدرت پرداخت است | لندینگ‌ها، CTA، قیف لید | Product/Marketing |
| D-011 | 2026-08-13 | Tier 3 به‌جای صفحهٔ فروش، موتور محتوایی دارد: ۲۰ مقالهٔ عمیق (۲۵۰۰–۴۵۰۰ واژه) برای تصمیم‌گیری جدی بعدی | قطعی | جلوگیری از فروش محصول ناقص و حفظ شفافیت پیام برند | فاز D، بلاگ/محتوا | Product/Marketing |
| D-012 | 2026-08-13 | معماری لندینگ‌ها Hub-and-Spoke با لینک متقابل است؛ هر صفحه CTA با پارامتر منبع و رویداد اختصاصی دارد | قطعی | قابلیت ردیابی نرخ تبدیل واقعی هر بخش در قیف | analytics, demo, leads | Product/Marketing |
| D-013 | 2026-08-13 | اتوماسیون سرتاسری AI (از اتصال تا تحلیل و انتشار مبتنی بر دادهٔ تصمیم‌ساز) خواستهٔ قطعی مالک است و D-006 را بازتعریف می‌کند | تصویب شد — معماری در سند ۳۴ (2026-08-14) | مالک: «تماما سیستم باید اتومیشن از روی داده‌های تصمیم‌ساز باشد»؛ پاسخ به ۵ سؤال مشورت ثبت شد | AI Gateway, publish policy, rollback, audit | Product/Security |
| D-015 | 2026-08-14 | مدل پیکربندی سهلایه: پیش‌فرض‌های سیستم → پروفایل‌های آماده (system presets) → شخصی‌سازی per-site در داشبورد؛ چند پروفایل هم‌زمان per site مجاز است | قطعی | مالک: «جدا از تنظیمات دیفالت و پروفایل‌های از پیش تعیین‌شده، تا حد ممکن قابل تنظیم در داشبورد» | automation_profiles، settings UI | Product/Security |
| D-016 | 2026-08-14 | دامنهٔ اتوماسیون کامل است: متا + محتوای متنی/مقالات + محصولات — همه خودکار با اجازه‌نامه، امتیاز اطمینان و rollback؛ بند «انتشار مستقیم محتوای AI ممنوع» در سند ۰۱ برای مسیر مجازِ دارای اجازه‌نامه اصلاح می‌شود | قطعی | مالک: «از محتوای متنی و مقالات تا محصولات تمام اتوماتیک، با شخصی‌سازی و چند پروفایل» | 01-Governance، command types، پلاگین | Product/Security |
| D-014 | 2026-08-13 | راه‌حل GSC ایران: ترکیبی — پروکسی API سمت سرور (زیرساخت موجود GSC_HTTP_PROXY) + مستندات VPS خارجی برای سازمانی؛ گزینه‌های ۵گانه در سند ۳۳ | معلق — پس از بستهٔ لندینگ | نیاز بازار ایران؛ تصمیم نهایی در سند ۳۵ | GSC, infra, deploy | Product/Tech |
| D-017 | 2026-08-15 | انتشار مستقیم (auto-publish) فقط با opt-in صریح دامنه: `site_automation_policies.auto_publish_scope` (none/meta/article/product/all) + گرمایش (متا=۳، محصول=۳، مقاله=۵ اجرای موفق انسانی) + گیت کیفیت محتوا با استاندارد مؤثر پیش‌نویس | قطعی | مالک: «گزینه‌ای برای انتشار مستقیم بگذار که سایت‌های کم‌حساسیت بتوانند استفاده کنند»؛ مسئولیت کیفیت با ما — گاردریل‌های عمیق برای به‌حداقل‌رساندن خطای AI | site_automation_policies، PolicyEvaluator، AutoPublish | Product/Security |
| D-018 | 2026-08-15 | استانداردهای محتوا (طول/هدینگ/عناصر الزامی/تن/راهنمای کلیدواژه) به‌صورت **دادهٔ نسخه‌دار** در جدول `content_standards` ذخیره می‌شوند (نه هاردکد در کد)؛ هر آپدیت استاندارد سئو نسخهٔ جدید می‌سازد (source: seed/learned/manual/serp) و `StandardsKB` استاندارد مؤثر را بر اساس نوع/زیرنوع/intent استخراج می‌کند | قطعی | مالک: «سیستم باید بفهمد چه چیزی و چرا تولید می‌کند؛ استانداردهای فعلی و KPIهای سایت را بداند و تک‌تک آپدیت‌های استاندارد سئو را ردیابی کند — نباید خودمان را با اعداد هاردکد محدود کنیم» | content_standards، StandardsKB، ContentQualityGuard، site_content_standard_learnings | Product/Content |
| D-019 | 2026-08-15 | گزارش تأثیر پس از انتشار (جایگاه/کلیک/نمایش GSC) فقط بر اساس **دادهٔ واقعی** سرچ کنسول ساخته می‌شود؛ بدون دادهٔ کافی → وضعیت `insufficient_data` با دلیل دقیق (هرگز تخمین/جعل عدد). خلاصه در کارت داشبورد «تأثیر محتوا» و ویجت جزئیات بازبینی | قطعی | اعتمادپذیری اعداد؛ گزارش باید «واقعاً چه اتفاقی افتاد» را نشان دهد نه پیش‌بینی | gsc_page_metrics، BuildPublishImpactReport، BuildContentImpactSummary، gsc:import | Product/Reporting |
| D-020 | 2026-08-16 | **قانون مستندمحوری (Doc-Driven Rule):** پروژه تماماً مستندمحور پیش می‌رود؛ هر تغییر مهم باید مستندات قبلی مرتبط را به‌روز کند و پیش از اعلام «تمام شد» همگام باشد؛ لاگ پیشرفت منبع حقیقت است | قطعی (الزامی) | درخواست مالک: «پروژه تماماً باید مستند محور پیش بره و بعد از هر اضافه/کم‌کردن/تغییر مهمی مستندات قبلی آپدیت بشن» | همهٔ مستندات + 31-Progress-Log | Governance |
| D-021 | 2026-08-16 | **برندینگ:** شرکت = **ویژن پرایم (Vision Prime)**؛ محصول = **سوئیت (SUITE)**؛ نام کامل فارسی = «**ویژن پرایم سوئیت**» (نه «سوئیت ویژن پرایم»). UI: «سوئیت» یا «Vision Prime SUITE»؛ مستندات: اولین ارجاع «ویژن پرایم سوئیت (Vision Prime SUITE)» سپس «سوئیت»؛ کپیرایت (شرکت) = Vision Prime | قطعی (اجرا شد — قدم ۰) | تصریح مالک: «خیلی جاها اسم محصول به Vision Prime اشاره می‌کند و هیچ اشاره‌ای به اینکه این پروژه سوئیت است نشده» + اصلاح ترتیب «ویژن پرایم سوئیت» | همهٔ UI/مستندات؛ فنی (دامنه/اسلاگ پلاگین/env) دست‌نخورده | Product/Brand |
| D-022 | 2026-08-16 | **ناوبری پنل مشتری = ۶ بخش با آیکون** (خانه/رشد من/اولویت‌ها/تأییدهای من/گزارش‌ها/وضعیت سایت) + زیرنویس غیرتخصصی هر بخش؛ صفحهٔ «فعالیت‌ها» در خانه ادغام شد (منوی مشتری دقیقاً ۶ بخش) | قطعی (اجرا شد — فاز B1) | تصمیم ۶ بارش فکری (ادغام فعالیت‌ها) + زبان روون برای کاربر غیرفنی | ClientNavigation.vue، سند ۰۵ | UX/Product |
| D-023 | 2026-08-16 | **دکمهٔ سوم «سؤال از تیم» در تأییدهای مشتری:** به‌جای ردکردن، مشتری دربارهٔ پیشنهاد سؤال می‌پرسد → ثبت در جدول `client_questions` + اعلان database (`ClientQuestionNotification`) به اعضای فعال سازمان (به‌جز خود سؤال‌کننده) + پیام موفقیت | قطعی (اجرا شد — فاز B5) | تصمیم ۹ بارش فکری: «کاربر غیرفنی که شک دارد به‌جای ردکردن سؤال می‌کند — تعامل بهتر» | client_questions، ClientQuestionNotification، ClientDecisionController@question | UX/Product |
| D-024 | 2026-08-16 | **داشبورد آژانس per-role:** هر نقش (مدیر/سئو/محتوا/بازبین/توسعه‌دهنده/بازاریابی/مدیر ارشد) «اولویت‌های نقش من» مخصوص خودش را می‌بیند؛ نقش‌های ناشناخته → «عضو تیم» عمومی (نه fallback به مدیر)؛ `currentRole` در Inertia shared | قطعی (اجرا شد — فاز C) | تصمیم ۴ بارش فکری: «مشتری کامل، آژانس پولیش وضعیت‌ها» + درخواست «هر نقش اولویت خودش را ببیند» | currentRole، App/Dashboard.vue، AppNavigation.vue | UX/Product |
| D-025 | 2026-08-17 | **دارک‌مود توکن‌محور:** حالت تاریک با اورراید متغیرهای سمنتیک در بلوک `.dark` (Tailwind v4 کلاسی با `@custom-variant`) — نه `dark:` پراکنده. کلید `VThemeToggle` در هدر هر دو پنل؛ انتخاب در localStorage (`suite-theme`، light/dark/system) + اسکریپت ضد-فلش در head. جفت‌های وضعیت (-50/-600) با هم flip می‌شوند | قطعی (اجرا شد — فاز D1) | تصمیم ۲ بارش فکری: «دارک‌مود قطعاً باشد، ولی با اولویت‌بندی» + معماری توکن‌محور فاز A | app.css، lib/theme.ts، VThemeToggle.vue، هر دو لایوت | UX/Foundation |
| D-026 | 2026-08-17 | **مرکز آموزش و دستیار پشتیبانی سهلایه:** کاتالوگ اکوسیستم در `lib/catalog.ts` (هر قابلیت: خلاصهٔ ساده + مزیت + گام‌های استفاده + لینک مستقیم) در صفحات `/app/training` و `/client/training`؛ دستیار پشتیبانی (`VSupportAssistant`) در هر دو پنل: FAQ → بات با دانش کاتالوگ (`config/assistant.php`) → پشتیبانی انسانی (کانال‌ها + فرم → `leads` با source=support). تور آنبوردینگ نیز با `storageKey`+`steps` تعمیم یافت و به پنل آژانس اضافه شد | قطعی (اجرا شد — فاز D2/D3) | تصمیم ۷ بارش فکری: «پشتیبانی اول از کاتالوگ/آموزش گام‌به‌گام، بعد بات با اشراف به کاتالوگ، در نهایت انسان» | lib/catalog.ts، TrainingCatalogView.vue، VSupportAssistant.vue، config/assistant.php، VOnboardingTour.vue | UX/Product |

## تصمیم‌های نیازمند پاسخ پیش از شروع توسعه مرتبط
| ID | سؤال | Deadline فاز | گزینه‌های اولیه |
|---|---|---|---|
| D-027 | 2026-08-17 | **فلسفهٔ «اتاق فرماندهی استثنامحور»:** پنل پلتفرم یعنی «سیستم خودش اجرا میکند، خودش خبر میدهد، فقط تصمیمهای واقعی به مالک میرسد» — معماری سهلایه (حسگر → Triage Engine → دست)؛ مالک هر روز درگیر جزئیات نشود؛ Daily Briefing + کانال فعال (تلگرام اول) + صف تصمیم | قطعی (تصویب شد — مبنا فاز E) | درخواست مالک: «قرار نباشه هر روز واردش بشم و درگیر کارهای خرد بشم؛ اکثر ساختار دادهمحور و اتومیشن» | سند ۳۷ (بخش ۳.۵)، ۳۸ | Product/Architecture |
| D-028 | 2026-08-17 | **فاز E (اتاق فرماندهی پلتفرم) با استک فعلی (Inertia+Vue) ساخته میشود — نه Filament/Nova:** پلن عمومی admin panel رد شد؛ محتوای خوب آن (فازهای بیزینس/مالی/امنیت) در E-رودمپ خودمان یکپارچه میشود؛ زیرساخت دادهٔ مالی E0 اجرا شد (plans/subscriptions/payments/invoices/platform_metrics + سرویسهای SubscriptionService/PaymentService/InvoiceService + seeder) | قطعی (اجرا شد — E0) | مالک: «زیرساخت مغایر قرار نیست بسازیم؛ پلن را با ساختار خودمان یکپارچه کن» | 38-Phase-E، app/Domains/Platform، BillingTest (۱۰ تست) | Product/Architecture |
| D-029 | 2026-08-17 | **هستهٔ اتاق فرماندهی پلتفرم (E1) ساخته شد:** لایهٔ `/platform/*` مجزا با middleware `platform.only` (فقط super-admin + audit) — داشبورد فرماندهی (KPI زنده + چارت + رویدادها)، **Triage Engine** (طبقهبندی normal/exception/decision)، صف تصمیم و **Daily Briefing** (گزارش صبحگاهی خودکار) — «سیستم خودش اجرا/خبر میدهد؛ فقط تصمیمهای واقعی به مالک» | قطعی (اجرا شد — E1) | پیادهسازی فلسفهٔ D-027 | platform/*، TriageEngine، SendDailyBriefing، PlatformAccessTest | Product/Architecture |
| D-030 | 2026-08-17 | **E2/E4 اتاق فرماندهی تکمیل شد:** impersonation با audit کامل و بلوک اکشنهای حساس (فقط مشاهده) + بنر قرمز در هر دو پنل؛ رصد فنی (صف/scheduler/اتصالها)؛ Emergency Stop سراسری (بالای per-site) با تأیید دوتایی؛ کانال تلگرام با fallback بیصدا؛ زنجیرهٔ Triage واقعاً تأیید شد (پرداخت failed → decision → صف تصمیم → resolve) | قطعی (اجرا شد — E2/E4) | تکمیل فلسفهٔ D-027 | PlatformImpersonationController، PlatformEmergencyStop، PlatformNotifier، Operations.vue | Product/Architecture |
| D-032 | 2026-08-17 | **فاز F (درگاه‌ها + SMS + AI Triage + MFA):** درگاه‌های چندگانه با قرارداد PaymentGateway (زرین‌پال v4 + آقای پرداخت + دستی — sandbox بدون کلید، تبدیل ریال→تومان در مرز درگاه) + پنل پیامکی (SmsSender + کاوه‌نگار + sms_logs + اتصال به DunningJob و یادآوری انتشار با sms_reminder_sent_at) + خلاصهٔ هوشمند Triage (AI با fallback قطعی آفلاین) + MFA سوپرادمین (TOTP RFC 6238 بدون کتابخانه + کد پشتیبان یک‌بارمصرف در UI + چالش ورود) | قطعی (اجرا شد — F01-F04) | تکمیل فاز E (D-031) | PaymentGatewayManager، SmsManager، AiTriageSummary، Totp، PlatformSmsController، PlatformMfaController | Product/Architecture |
| D-031 | 2026-08-17 | **فاز E کامل شد (E3/E5):** مدیریت عملیاتی پلن/اشتراک/پرداخت/فاکتور + DunningJob (معوق → grace → تعلیق) + گزارشهای درآمد/رشد/سلامت + Export CSV + بکاپ روزانهٔ DB (platform:backup-db) — اتاق فرماندهی استثنامحور از «دیدهشدن» تا «عمل مالی» و «خودبکاپ» کامل شد | قطعی (اجرا شد — E3/E5) | تکمیل فلسفهٔ D-027 | PlatformBillingController، DunningJob، PlatformReportController، BackupDatabase | Product/Architecture |
| D-033 | 2026-08-17 | **گزارش هفتگی مالک (هر جمعه) + MFA اختیاری + رفع منوی پلتفرم:** SendWeeklyReport — خلاصهٔ مالی/سلامت/تصمیمهای باز از طریق ایمیل + تلگرام + پیامک (OWNER_PHONE اختیاری، بدون آن بیصدا)؛ MFA با تنظیم پلتفرمی mfa_required (پیشفرض خاموش) کاملاً اختیاری شد + کلید «الزام» در صفحهٔ امنیت؛ MFA تستی اکانت سوپرادمین پاک شد؛ منوی پلتفرم در موبایل با همبرگری + دراور درست شد | قطعی (اجرا شد — ۲۰۲۶-۰۸-۱۷) | مالک: «قابلیتهای دومرحلهای را اختیاری کن» + گزارش هفتگی | SendWeeklyReport، PlatformSettingsService، EnsureMfaVerified، PlatformLayout، WeeklyReportAndMfaTest | Product/Architecture |
| D-034 | 2026-08-17 | **استقرار تولید روی سرور [REDACTED] + اتصال دامنهٔ visionprime-suite.ir:** کل پروژه (کد + دیتابیس واقعی) روی Ubuntu 24.04 مستقر شد (PHP 8.3-fpm + nginx + صف database + cron)؛ رکوردهای A آمادهٔ ثبت در ایرنیک (دامنه ثبت شده ولی DNS ست نشده)؛ زیرساخت سایت‌های استاتیک (/var/www/sites + الگوی vhost) برای استقرار دونه‌به‌دونهٔ سایت‌های بعدی آماده شد | قطعی (اجرا شد — ۲۰۲۶-۰۸-۱۷) | مالک: «همهٔ پروژه را روی سرور مستقر کن و به دامنه متصل کن + سایت‌های استاتیک بعدی» | سند ۴۱، /var/www/sites، static-template | Architecture/DevOps |
| D-035 | 2026-08-18 | **سرویس ایمیل دامنه (Postfix+Dovecot+Roundcube) برای اینماد و ارتباطات:** سرور ایمیل کامل روی [REDACTED] — صندوق‌های `info@` و `admin@` (احراز passwd-file + TLS)، ارسال از submission 587/465 با AUTH PLAIN + امضای DKIM (opendkim, selector mail)، رکوردهای MX/SPF/DKIM/DMARC در زون bind9، وبمیل فارسی Roundcube در `mail.visionprime-suite.ir`؛ گام باقی‌مانده: انتشار NS دامنه در ایرنیک تا ایمیل از بیرون برسد | قطعی (اجرا شد — ۲۰۲۶-۰۸-۱۸) | مالک: «برای اینماد نیاز به ایمیل دامنه دارم — بسازش» | scripts/setup-webmail.sh + سند ۴۱ (بخش ۱۲) | DevOps |
| D-036 | 2026-08-18 | **سیستم دیپلوی خودکار سایتهای استاتیک:** `deploy-site.sh` (گیت/zip/پوشه + rsync --checksum + لاگ) + کنسول وب فارسی در `http://[REDACTED]/deploy/` (توکن از /etc/deploy-secret) + وبهوک گیتهاب (HMAC-SHA256 از /etc/deploy-webhook-secret، نیاز به `underscores_in_headers on`) + نصب روی www-data با HOME=/var/www/.deploy-home؛ تأیید کامل: چرخهٔ zip (V1↔V2)، git clone/pull، وبهوک امضای درست → deploy، امضای غلط → 403، توکن غلط → رد | قطعی (اجرا شد — ۲۰۲۶-۰۸-۱۸) | مالک: «یک سیستم دیپلوی خودکار برای سایتهای استاتیک بساز» | scripts/deploy-site.sh + scripts/deploy-console.php + سند ۴۱ (بخش ۱۳) | DevOps |
| O-001 | مدل استقرار مشتری چیست؟ | Phase 0 | SaaS چندمستاجری / private deployment / self-hosted |
| O-002 | مدل قیمت‌گذاری و entitlement چیست؟ | Phase 3 | per site / per seat / per agency / usage |
| O-003 | export گزارش چگونه است؟ | Phase 11 | PDF / secure link / email / all |
| O-004 | اولویت providerهای AI چیست؟ | Phase 9 | OpenAI-compatible / local/self-hosted / multiple providers |
| O-005 | چه Commandهای R1 و R2 در نسخه تجاری مجازند؟ | Phase 10 | allowlist نهایی با تیم محصول/حقوقی |
