# Changelog — Vision Prime SUITE

تمام تغییرات مهم این محصول در این فایل ثبت می‌شود.
قالب: [Keep a Changelog](https://keepachangelog.com/fa-IR/) · نسخه‌بندی: [SemVer](https://semver.org)

## [1.0.0-rc1] — ۲۰۲۶-۰۸-۲۶

اولین کاندید انتشار عمومی (Release Candidate). محصول از «اسکلت» به «پلتفرم تست‌شدهٔ تجاری» رسید.

### اضافه شد (Added)
- **احراز هویت کامل:** ثبت‌نام/ورود با کد یکبارمصرف پیامکی (کاوه‌نگار + حالت sandbox)، نقش‌ها و ~۵۰ مجوز دانه‌ریز (RBAC)
- **کانکتور وردپرس:** pairing با توکن یک‌بارمصرف، امضای HMAC-SHA256 + nonce + محافظت replay، رمزنگاری AES-256-GCM سcret، ۶ نوع فرمان (شامل انتشار مقاله/محصول)
- **تولید محتوا با AI:** زنجیرهٔ failover سه‌مرحله‌ای (provider سازمان → ۸ مدل رایگان OpenRouter → موتور قانونی)، ۲۲ گیت کیفیت محتوای فارسی
- **موتور اتوماسیون:** انتشار خودکار با گیت‌های چندلایه (scope، گرمایش ۵ اجرا، کیفیت، امتیاز اطمینان ۴عاملی)، توقف اضطراری، rollback خودکار، حلقهٔ یادگیری
- **تقویم محتوایی، پرتال مشتری، GSC (OAuth واقعی گوگل)، هوش سئو، پنل سوپرادمین (MFA، impersonation)، مارکتینگ + لیدها**
- **P2 تجاری:** سقف‌های پلن (سایت/مشتری/توکن AI) روی مسیرهای تجاری؛ درگاه زرین‌پال + عقای + دستی؛ اشتراک/فاکتور
- **`demo:seed`:** محیط دموی فروش در یک فرمان + تست دود لاگین
- **`app:health`:** گزارش سلامت روزانه (صف، خطاها، دیسک، بکاپ) با خروج بحرانی برای alert
- **تست ایزولاسیون چند-مستأجری:** ۱۹ سناریوی حملهٔ بین‌سازمانی + محافظ دائمی مسیر پول (E2E)

### امنیت (Security)
- رفع: هر آژانسی می‌توانست review آژانس دیگر را تأیید کند (DecideReviewItem)
- رفع: endpointهای عمومی کانکتور بدون rate-limit بودند
- رفع: کلید APP و IP سرور از مخزن حذف شدند (چرخش کلید الزامی پیش از بهره‌برداری — سند ۴۹)
- سخت‌سازی: سقف روزانهٔ تولید AI به ازای هر سازمان، گارد دمو در production

### رفع شد (Fixed)
- قیف ثبت‌نام کاملاً شکسته بود (کنترلرهای OTP استاب 501 بودند)
- تولید مقاله روی SQLite کرش می‌کرد (سینتکس فقط-PostgreSQL)
- گیت یکتایی، محتوای تازه را «۱۰۰٪ تکراری» می‌دانست
- امتیاز اطمینان انتشارِ تأییدشدهٔ انسانی روی لبهٔ آستانه می‌ماند (84/85)
- پرداخت موفق، اشتراک را فعال نمی‌کرد
- CI هرگز اجرا نمی‌شد (workflowها خارج از ریشهٔ مخزن بودند)

### تغییر کرد (Changed)
- `ContentApiController` ۱۴۱۴خطی → ۶ کنترلر دامنه‌ای؛ پرامپت‌های AI به `AiPromptBuilder` مستقل
- workflowهای CI به ریشهٔ مخزن منتقل شدند؛ بیلد پلاگین از سورسِ خوانا روی هر تگ
- وردپرس خام (۱۴۵MB) از مخزن untrack شد

### زیرساخت
- ران‌بوک عملیاتی سرور (سند ۴۹): SSL با چالش DNS-01، cron+Supervisor، مهاجرت PostgreSQL، بکاپ شبانه
- `server-bootstrap.sh`: راه‌اندازی یک‌فرمانی سرور (بکاپ → دیپلوی → Worker → بکاپ خودکار → فایروال)

### شناخته‌شده (Known Issues)
- دیتابیس پروداکشن فعلی SQLite است (مهاجرت به PostgreSQL طبق سند ۴۹ الزامی)
- SSL تا انتشار NS دامنه در ایرنیک فعال نیست
- پرداخت آنلاین پشت feature-flag (`payments_enabled`) تا تأیید سندباکس
- مانیتورینگ خارجی (Sentry/uptime حرفه‌ای) هنوز متصل نیست

## [0.9.0] — ۲۰۲۶-۰۸-۰۱
اسکلت اولیهٔ پروژه (Laravel 12 + Vue 3 + Inertia + پلاگین وردپرس).
